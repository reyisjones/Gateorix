/**
 * `gateorix dev` — start the app in development mode.
 */

export async function devCommand(): Promise<void> {
  console.log("\n  Starting Gateorix in development mode...");
  console.log("  → Frontend dev server: http://localhost:5173");
  console.log("  → Host core: starting...");
  console.log("  → Runtime adapter: starting...\n");

  // TODO: Phase 2 implementation
  // 1. Start the frontend dev server (vite)
  // 2. Start the host core process
  // 3. Start the runtime adapter sidecar
  // 4. Watch for file changes and hot-reload

  console.log("  [dev command not yet implemented — coming in Phase 2]");
}
