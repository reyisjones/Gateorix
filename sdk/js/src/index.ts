/**
 * @gateorix/bridge — frontend SDK for Gateorix applications.
 *
 * This library provides the API surface that frontend code uses to
 * communicate with the Gateorix host core and runtime adapters.
 */

export { GateorixBridge } from "./bridge.js";
export type { IpcRequest, IpcResponse, IpcEvent, InvokeOptions } from "./types.js";
